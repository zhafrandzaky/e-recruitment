# Backend API base URL (must match the Laravel app's APP_URL + /api)
VITE_API_BASE_URL=http://localhost:8000/api

# WebSocket connection — must match apps/api Reverb config
VITE_REVERB_HOST=localhost
VITE_REVERB_PORT=8080
VITE_REVERB_APP_KEY=
